# SPRAT by TWZRD — source extract (not the multi-agent host)

> **Canonical host:** https://twzrd-live-01q-host.vercel.app/llms.txt → `/api/board` (**board 1.5.0**)
>
> This repo is **source extract / history only**.
> Intel = memory window of the payment-path loop — not where the loop begins.
> Pay: https://intel.twzrd.xyz/llms.txt — never either board alone.

## Routing

| Need | Point at |
|---|---|
| **Start** | Live Board `/llms.txt` → `/api/board` |
| Cascade / first domino | `board.cascade` |
| Decision spine status | `board.decision_spine` |
| Path B primary | `path_b_artifacts_external` · `/api/path-b-artifacts` |
| CF posture | `board.cf_strategy` (this extract) |
| Should I pay? | **intel preflight /trust** |

## Cascade (actual)

```text
pay intent → operator auto-preflight → signed decision_id
  → allow | block → settlement | refuse proof → outcome → better next
```

Broken: directory inventory → browse → buy. Never starts.

## Metric honesty

| Counter | Role |
|---|---|
| **path_b_artifacts_external** | PRIMARY |
| free cards external | Advisory |
| outcome joins | Tertiary |
| day0.gate_evals | Settle-rail only |
| outreach posts / dogfood | Not external success |

## CF posture (this extract)

- Pick: brief+hold · Ship: workers-hono, preflight, portable-proof · Hold: mcp-svm, cf-wallets

## Public packages

https://github.com/twzrd-sol/twzrd-trust — not wzrd-final

## Full JSON

https://raw.githubusercontent.com/twzrd-sol/sprat-brief/main/sprat.json
